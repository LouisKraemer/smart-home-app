const INITIAL_STATE = {
  connected: false
};

const websocketReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { websocketReducer };
